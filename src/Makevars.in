# Define paths to our compiled static libraries
# Use .libs/ for Unix-like systems
HDF5_LIB = ./hdf5-1.14.6/src/.libs/libhdf5.a
ZLIB_LIB = ./zlib-1.3.1/libz.a

# Tell the R linker to link against both libraries
# NOTE: The Unix 'configure' script adds '-lpthread -ldl' to this line
PKG_LIBS = $(HDF5_LIB) $(ZLIB_LIB)

# Tell the R compiler where to find HDF5 headers AND undefine macros
# for features disabled during HDF5 configure.
PKG_CPPFLAGS = -I./hdf5-1.14.6/src -I./hdf5-1.14.6/hl/src \
               -UH5_HAVE_SUBFILING_VFD -UH5_HAVE_DIRECT_VFD