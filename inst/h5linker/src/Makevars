# src/Makevars for h5linker package

# Get the C/C++ compiler flags from hdf5lib
PKG_CPPFLAGS = `$(R_HOME)/bin/Rscript -e "cat(hdf5lib::c_flags())"`

# Get the linker library flags from hdf5lib
PKG_LIBS = `$(R_HOME)/bin/Rscript -e "cat(hdf5lib::ld_flags())"`