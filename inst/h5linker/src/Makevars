# src/Makevars for h5linker package

R_HOME      ?= $(shell R RHOME)
PKG_CPPFLAGS = $(shell $(R_HOME)/bin/Rscript -e "cat(hdf5lib::c_flags())")
PKG_LIBS     = $(shell $(R_HOME)/bin/Rscript -e "cat(hdf5lib::ld_flags())")